{
  "name": "learning_npm",
  "version": "1.0.0",
  "description": "learning about npm",
  "main": "index.js",
  "type": "module",
  // The type of project is module based so we can use imports and ES modules instead of using the dufault which is common js 
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "eyad_elorfi",
  "license": "ISC",
  "dependencies": {
    "sillyname": "^0.1.0"
  }
}
